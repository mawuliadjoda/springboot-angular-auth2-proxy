server:
  servlet:
    context-path: /resource-server

spring:
  application:
    name: resource-server


  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:7080/realms/master"