apiVersion: v1
kind: Service
metadata:
  name: {{ include "resource-server-chart.fullname" . }}
  labels:
    {{- include "resource-server-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "resource-server-chart.selectorLabels" . | nindent 4 }}
