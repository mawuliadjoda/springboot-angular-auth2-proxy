apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "resource-server-chart.fullname" . }}-test-connection"
  labels:
    {{- include "resource-server-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "resource-server-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
